server:
  host: "0.0.0.0"
  port: 8080

cors:
  enabled: true
  allowedOrigins:
    - "*"
  allowedMethods:
    - "GET"
    - "POST"
    - "DELETE"
    - "OPTIONS"
  allowedHeaders:
    - "Content-Type"
    - "Authorization"

logging:
  level: "info"
  format: "console"
